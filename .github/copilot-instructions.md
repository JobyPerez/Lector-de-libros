# Instrucciones del repositorio

- Después de cualquier cambio en este repositorio, ejecuta `npm run build` desde la raíz del workspace.
- Cuando el build termine correctamente, ejecuta `pm2 restart conejolector-api`.
- No des una tarea por terminada si alguno de esos pasos falla; informa el error al usuario.